package LandingSimulation;

/**
 * main uses the selected solving method (ODE solver)
 * and inputs the numbers stored in the two vectors into the selected formula in order to update
 * the crafts state. Then, a timer allows for the repetition of this procedure. After each function
 * evaluation the information stored in the two vectors is passed to the graphics class which is
 * making everything visual in real time.
 */
public class main {
    public static void main(String arg[]) {
        OpenLoop control = new
    }
}
